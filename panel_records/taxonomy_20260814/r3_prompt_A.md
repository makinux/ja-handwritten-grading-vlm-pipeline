# ラウンド 3: 最終立場

あなたはこの設計パネルのパネリストである(最終ラウンド)。下に「あなたへの批判(相手のラウンド 2)」と「ファシリテータ検証済み事実の追補(V6〜V9)」が続く。あなたの最終メッセージが討論記録としてそのまま保存される。前置き不要。日本語。

## 指示

1. **正当な批判には理由付きで譲歩せよ**(社会的譲歩ではなく、論拠による譲歩のみ)。
2. **生き残る主張は理由付きで防衛せよ。**
3. 確信度を変えた場合、**それを動かした具体的論拠を必ず明記**せよ(説明なしの全面転向は不可)。
4. 残余不確実性を明示し、各中核主張に較正済み確信度と実行可能な反証条件を付けよ。
5. D1〜D5 の最終立場を、裁定者がそのまま採用できる**決定文の形**で書け(「〜すべき」ではなく「〜する。条件は〜」)。
6. 相手と合意に達した項目は簡潔に列挙し、**残る真の対立**があれば「何の証拠があれば決着するか」を付して明示せよ。

---
# あなた自身のラウンド 2(現在の立場)

## 無意味化チェック

V1〜V3により、**現行データの旧9クラスを維持してモデル改善で解くべきか**という争点は決着済みである。移項は95.6%が観測同値、誤解の語りは0件、統合後HFマクロは0.970である。したがって、旧下位区分の成績をモデル能力の指標として報告すること、旧下位区分に対するRL報酬を議論することは現段階では無意味である。

この点を受け、私のラウンド1案も修正する。**現行ラベルの第2階層は診断指標としても報告せず、ラベル衝突のデータ監査値としてのみ残す。** 一方、再生成後の観測証拠ラベル、積ペア、深刻度、学習者集約はV1〜V3では決着していない。

## Bの数値・論理への反駁

### 1. 「理論上限7/9」は前提不成立

Bは、

> 「2対が完全衝突する場合、9クラスマクロ一致の理論上限は7/9 ≈ 0.778」  
> 「実測0.760は上限の97.7%。モデルは既に天井にいる」

とした。

完全衝突という仮定の下では、再計算結果は一致する。

- \(7/9=0.7778\)
- \(0.760/(7/9)=0.9771\)、すなわち97.7%

しかしV1では完全衝突ではなく、移項0.956、積0.423である。Bと同じ単純化を部分衝突へ延長し、非衝突部分を完全正解できると仮定すると、

\[
\frac{5+(2-0.956)+(2-0.423)}{9}
=\frac{7.621}{9}
=0.8468
\]

となり、0.778ではない。入力分布や機械側の曖昧領域まで仮定しないと厳密な上限自体を出せないため、「理論上限」「既に天井」は過剰な断定である。言えるのは、**現行KPIがラベル衝突に強く支配されている**ことまでである。

### 2. 統合規則と最終推奨が内部矛盾している

Bは本文で、

> 「ρ ≥ 0.5の対を統合する」

とした一方、自説の最弱点では、

> 「0.4 ≤ ρ < 0.7ならG3のみ導入して再生成後に再判定」

としている。ρが0.5以上0.7未満なら、前者は統合、後者は再生成であり、規則が正面から矛盾する。

さらにBの単一推奨は、

> 「既知の2対を統合すれば7クラス」

であるが、積ペアの実測ρは0.423である。これはB自身のどちらの規則でも即時統合帯ではない。積概念855件のうち観測同値は362件、**非同値は493件、57.7%**ある。7クラス化はこの信号を無検証で捨てる。

対して移項は非同値が34/765、4.4%しかない。したがって、ペア別判断をせず一括して7クラス化する推奨は成立しない。

### 3. 0.95という閾値は「推測で決めない」と整合しない

Bは、

> 「閾値を0.85→0.95に引き上げる」  
> 「値は推測で決めず、既存の行別ログの再集計だけで確定できる」

とした。

V3の再集計値0.970は一致した。しかし、それは同じHF評価ログに対する事後集計であり、独立テストでもllama.cppでもない。0.970を見込んで0.950を置くことは、値をデータから「確定」したのではなく、**単一評価結果へ閾値を合わせた**だけである。新しい固定層化セットと両ランタイムで分散を測るまでは、0.95をリリースゲートにする根拠はない。

### 4. 件数引用は不正確

Bの、

> 「op-transpose-sign:18 vs op-llm-transpose-misconception:14」  
> 「op-llm-product-misconception:21 vs op-mult-sign-drop:3」

はV4と一致しない。正しくは、評価GTで移項22対13、積20対3である。

- 移項比：22/13 = 1.69
- 積比：20/3 = 6.67

多数・少数という定性的説明は残るが、「拮抗だからランタイム間反転、7:1だから少数側消失」という定量的な説明は、誤った件数のまま提示されていた。

### 5. 「label-by-construction違反」は概念を混同している

Bは、

> 「これはlabel-by-constructionという根本原理の違反」

とした。

生成オペレータを表すprovenanceラベルとしては、構成上のラベルは正しい。壊れているのは、**そのprovenanceを観測入力から予測させる教師ターゲットとして採用したこと**である。これは「ラベル生成の誤り」ではなく「予測対象の識別不能性」であり、修正先を区別すべきである。

同じ理由で、

> 「誤りサンプルの約2割にcoin-flipラベル」

も数値的に定まらない。衝突した概念側だけなら、\((731+362)/7000=15.6\%\)。対応する機械側全件まで曖昧領域と数えれば、\((731+362+833+354)/7000=32.6\%\)である。また積は事前分布が偏っておりcoin-flipでもない。

### 6. 「必ず丸暗記」「確実に崩れる」は未検証

Bは説明注入について、

> 「モデルはその語り句を丸暗記して分類する最短経路を必ず取り」  
> 「sim2realで確実に崩れ」

と断言した。しかし同じ回答で確信度70%とし、実答案30枚によるR5を反証条件にしている。未測定の実分布に対して「必ず」「確実」は内部整合しない。

また私の案は定型句の付与ではなく、未見表現の対照評価と、独立した複数箇所での一貫適用を含む。Bが否定した「素朴版」は、私の提案全体への反駁になっていない。

### 7. 「生徒ID保持は開始前なら無料」は誤り

Bは、

> 「収集開始前なら無料」

としたが、生徒単位の連結には同意、匿名化、アクセス制御、離脱処理、偏り管理が必要であり無料ではない。必要なのは原則として実IDではなく、適切な同意の下での**仮名化された学習者または収集セッションキー**である。「今決めないと不可逆」という点から、無条件のID保持までは導けない。

## 裁定

### D1：現時点では段階の違い。ただし将来の構成概念は異なる

現行データについてはBを採る。旧「概念／記法」下位区分は封印し、参考モデルKPIにも使わない。

ただしBの、

> 「教育的価値は誤りインスタンス階層ではなく学習者階層に存在する」

という排他的主張は採らない。答案中に誤った一般則が明記されていれば、それは単一誤りにも属する観測証拠である。一方、複数題での反復から推定する「学習者が概念的誤解を持つ」という診断は学習者階層である。

したがって、

- 誤り階層：`family`と`misconception_evidence=explicit/repeated/unknown`
- 学習者階層：複数題を集約した概念診断

に分ける。現時点の対立は主に段階差だが、この二つを同一視するかについては実質的対立が残る。

### D2：現行は撤去、下位区分復活後は部分報酬を再導入

現行ターゲットがファミリだけなら「上位階層一致」の部分報酬は定義不能なので撤去する。

観測可能な第2階層を再導入した後は、ファミリ一致は当てずっぽうではないため部分報酬を使える。ただし、粗いfamily出力だけで満点近く取れないよう、完全一致より明確に低い報酬とする。Bの撤去は現段階には妥当だが、恒久撤去にはならない。

### D3：一律sev2を否定

統合ファミリを一律sev2にする根拠はない。観測不能な原因区分に連動したsev2/sev3をVLM教師ラベルから外す点までは必要だが、正しい代案は恣意的なsev2への格下げではない。

深刻度を使うなら、失点、後続ステップへの伝播、解答不能化など観測可能な影響から独立に定義する。定義できない間は`unknown`または出力対象外とする。sev3の再導入を下位区分復活に結び付けるのも、深刻度と原因を不必要に結合している。

### D4：積ペアはG3導入・再生成後に再判定

積ペアρ=0.423では即時統合しない。G3で観測同値候補を除外し、さらに概念根拠を観測可能な証拠として生成した上で再訓練・再評価する。493/855の非衝突例を捨てる前に識別可能性を試すべきである。

移項ペアはρ=0.956かつ説明信号0件なので、現行データでは即時統合する。積±1/2は91/855=0.106で、サイト・スパン同値も未確認であるため統合対象ではなく監査候補に留める。

### D5：条件付き採用

`errors[].type`には局所的に観測できる誤りファミリを残す。概念診断は、複数題を集約できる場合に限り別の学習者階層出力へ移す。

ただし、単一答案中の明示的誤規則まで集約出力だけに追い出す必要はない。これは`errors[].evidence_basis`などで局所証拠として保持できる。よってBの出力契約批判は、**familyは局所出力、学習者診断は集約出力**という条件で採用する。

## 真の合意点

- 現行の旧下位区分はリリースKPIに使えない。
- 現行移項ペアはファミリへ統合する。
- `reason`が破棄され、誤解の語りが存在しない。
- 積ペアはG3導入と再生成後に再判定する。
- 複数題を使う学習者階層の概念診断は、局所誤り分類とは分離する。

---
# あなたへの批判(パネリスト B のラウンド 2)

# パネリスト B ラウンド 2

(注: B は a_r2_raw.md の存在に気づいたが、相互批判の対称性を保つため意図的に読まず、a_r1.md のみを攻撃対象とした)

## 0. 無意味化チェック(攻撃の前に)

V1〜V3 によって既に決着している論点を先に落とす。

- **ブリーフの (a) vs (b) 軸は死んだ。** V2 で語りは 0/765・0/833、コード経路も確認済み。(b) は「薄い信号を強める」案ではなく「存在しない信号をゼロから注入する」案である。両案の比較の前提が消えた。
- **A の必須 KPI は測定によって既に達成されている。** A の第1階層(演算順序/移項/符号・乗算/符号落とし/加減/加減乗除/除算)は V3 の統合写像と集合として同一である。したがって V3 の 0.970 は**そのまま A の第1階層マクロの実測値**である。A の「必須 KPI:第1階層のクラス別一致マクロ ≥ 0.85」は、工数ゼロ・追加訓練ゼロで 0.12 の余裕をもって充足済みであり、何もゲートしない。A は V3 を見る前に書いているのでこれは A の失策ではない。ただし **A 案の運用部分の中核は V3 によって空洞になった**。
- **D2 は独立の対立ではない**(後述)。
- **D3 は条件付きでのみ生きている**(深刻度をモデル出力契約に入れない限り、RL 重み/フィードバック優先度に使う決定を先にしないと動かない)。

生きているのは **D1 の criterion 1**、**D4**、**D5** の三つだけである。

## 1. A への攻撃

### 攻撃1:`概念根拠あり` の criterion 1 は、A 自身が同じ文書で否定した失敗モードそのものである

A は第2階層の構成要件をこう定義する。

> `概念根拠あり` は、次のいずれかが答案中に観測できる場合に限定する。
> 1. 「移項しても符号は変わらない」等、誤った一般則を明示している。

そして同じ回答の 5 行下でこう書く。

> (b) 単独では不十分。概念例だけに定型句を追加すると、概念理解ではなく語句検出の合成ショートカットになる。

criterion 1 が答案中に観測できるようになる経路は、A 自身が「次の生成・訓練サイクルで (b) を実施する」と宣言している (b) しかない。つまり A は、自分が合成ショートカットだと断じた操作を、第2階層ラベルの**構成的定義**に据えている。「(b) 単独では不十分」という留保は矛盾を解消しない。criterion 2 を併置しても、criterion 1 で作られたサンプルは依然として語句検出だけで正解できるからである。

さらに致命的なのは昇格条件の 4 番目である。

> 同じ誤り式に対して、説明・反復文脈だけを変えた対照ペアの判定正解率 ≥ 0.90

このゲートは、**純粋な語句存在検出器が 1.00 で通過する**。誤り式を固定し説明句の有無だけを振ってラベルが従属するなら、説明句を見るモデルが最高得点を取る。A の昇格ゲートは、A が排除したいと言った失敗モードによって最も効率的に突破される。ゲートとして機能していない。

想定される A の再反論——「再定義したのだから語句検出こそが正しい振る舞いだ」——も成立しない。それを認めるなら、ラベルは transcript の決定論的関数になり、CER≈0 の現状では課題は OCR+正規表現に還元される。VLM を評価する意味が消える。

### 攻撃2:criterion 1 のクラスは、計画中のどの実データにも支持体を持たない

これは A の見落とした事実である。`docs/collection_plan.md` を読むと、1,350 枚の内訳は 書き写し 900 + 公平性 330 + 自力解答 120 であり、前二者は**合成誤りの書き写し**、唯一の自発筆記である自力解答 120 枚には計画書自身がこう書いている(同 13 行)。

> 成人は中学数学で概念誤りを生まないため統計判定には使わない(パネル合意)

したがって「生徒が誤った一般則を答案に明示する」という事象を観測できるデータは、現行計画のどこにも存在しない。criterion 1 は、合成データでは注入によってのみ存在し、実データでは支持体ゼロのクラスである。sim2real で崩れるのではなく、**配備時に空になる**。A が (c) を否定した理由は正しいが、criterion 1 はこの問題を解いていない。観測不能な provenance を、実分布と無相関で構成上のみ完全相関する代理特徴に置き換えただけである。

### 攻撃3:A の反証条件は妥当性の項を欠いており、しかも自分の別節と矛盾する

A の最弱点節の反証条件(教員 2 名・100 答案・κ≥0.70・一致率≥0.80)について: κ も一致率も**評価者間指標**であって妥当性指標ではない。V1 で移項ペアの表面一致率は 0.956 である。教員 2 名に見せられる 100 枚のうち約 95 枚は文字列として同一であり、両者が「迷ったら記法」という共通の既定規則を持つだけで κ は 1.0 に近づく。**κ は最大化され、構成ラベルに対する正解率は 50% 付近に留まる**という結果が最も起こりやすい。A の反証条件は、A の主張が正しいときにこそ通過しうる。

しかも A は主張 1 の反証条件では正しく妥当性の項(balanced accuracy ≥0.85)を入れている。同一人物が、同一文書内で、同種の人手検証に対して片方には BACC を課し片方には課していない。内部不整合である。そして BACC を課した側の条件は V1 によって**既に決着済み**——移項ペアの人間 BACC 上限も 0.52 前後であり、0.85 は人間にも到達不能。

### 攻撃4:A の第1階層写像は衝突を閉じていない(コード由来の新事実)

`pipeline/gen_core.py:237` は op-arith-slip のサイトを **s2/s3 の 1/2 抽選**にしている。s2 が引かれたとき `mv = q*r + delta` を書き、これは 概念/符号・乗算 が `q*r±1,2` を提案した場合と**同一サイト・同一スパン・同一下流**になる。したがって V1 の但し書き「op-arith-slip の変異サイトは通常別ステップ」は**誤り**である。約半数が同一ステップである。このプールの少数側 91 件(9.1%)は原理的に回復不能で、**A の第1階層では 符号・乗算 と 加減乗除 という別ファミリに跨る**。A の統合はファミリ内衝突を潰したが、ファミリ**間**衝突を残している。

ファシリテータへの検算要求:③ を `op-arith-slip` の `site == "s2"` で条件付けて再カウントされたい。

### 攻撃5:A のコード読解は正しいが、そこからの推論の種類を間違えている

A の「誤解の語りは生成され得ても、必須化されてはいない」は頻度・強制の問題という含みを持つが、実際は**構造的に不可能**(0/765、V2)である。「必須化されていない」なら現データにも一定量の語りが混じっており下位区分の一部は正当に学習可能という読みが成り立つが、実際はゼロなので、現行 9 クラス評価で観測された下位区分の数値は**すべて曖昧領域のタイブレークの記録**であり、部分的にでも意味のある信号を含まない。

### 攻撃6:見落とされた代替案——A の「10 時間」は不要である

衝突の解消は再生成を要しない。衝突判定述語は既存レコードの `params` と `payload` から計算できる。**GPU ゼロ・再生成ゼロ・再訓練ゼロの再ラベル**で、既存 gen10k をそのまま識別可能粒度へ揃えられる。A は再生成を前提に設計判断を組んでいるため、より安い手を検討していない。

### 攻撃7:criterion 2 は集合レベル述語であり、A の第2階層ラベルには載らない

「2 箇所以上で一貫」は誤り**集合**の上の述語なので、誤り i のラベルが誤り j の存在に依存する。帰結: (1) 誤り単位のスコアリングが独立でなくなる。(2) 誤り単位の RL 報酬が分解できない。(3) **検出誤差と分類誤差が結合する**——2 箇所のうち 1 箇所を検出し損ねただけで、残る 1 箇所のラベルが `概念根拠あり` から `原因不明` へ反転する。criterion 2 を採るなら、誤り要素フィールドではなく答案/学習者レベルの出力に置くしかない。

## 2. 自説の訂正(再計算による)

### 訂正1:F3 の上限は誤りだった。ただし結論は強化される

r1 の「理論上限 7/9 ≈ 0.778」は両ペア ρ=1.0 仮定の誤り。V1 実測と V3 内訳で再計算:

- 非対ペア 5 クラス和 4.833(V3 実測を到達水準として使用)
- 移項ペア(22/13、ρ=0.956): 上限和 **1.044**
- 積ペア(20/3、ρ=0.423): 正解率最適 0.957 / マクロ最適 1.509(ファミリ上限 0.957 乗算後)

9 クラスマクロ上限: マクロ最適 **0.821**、正解率最適 **0.759**。実測 0.760 は**正解率最適上限と 3 桁一致**。モデルは天井にいる。0.85 は理想化上限 0.821 でも届かない。

### 訂正2:F5 の件数と機構

件数は V4 が正しい(22/13、20/3)。18/14・21/3 を撤回。機構も訂正: 曖昧プール内の比は移項 833:731=1.14:1、積 354:362=**1.02:1** でほぼ完全な拮抗。「7:1 の事前分布で少数側が潰れた」という説明は成立せず、割当は純粋に任意でランタイム間で反転しうる。「計算/符号 0/3」は n=3 で検出力がなく証拠として使うべきではなかった。

### 訂正3:coin-flip ラベルの割合

「約 2 割」は目分量。正確には曖昧領域 2,280/7,000 = **32.6%**、うち原理的に回復不能(少数側)1,085/7,000 = **15.5%**。

### 訂正4:衝突候補③の予測方向が逆だった

③が損なうのは加減乗除の **precision** であって V3 の **recall** ではない。「その他クラス下限 0.83 の説明」は取り下げる。

### 訂正5:収集計画についての事実誤認(r1 提案 7 と R5 を撤回)

「生徒 ID 保持を今すぐ要件化」は**事実誤認**——collection_plan.md の割付は既に書き手 ID 付き・複数枚/書き手であり、スキーマは存在する。しかし真の律速は別: 書き写し 900+公平性 330 は合成誤りの書き写しで集約診断の substrate にならず、唯一の自発筆記 120 枚は成人(概念誤りを生まない・統計判定から除外済み)。**復活条件 (ii) の検証手段は現行計画に存在せず、不足はスキーマではなく中学生書き手**(collection_plan.md:50 に未決のまま、所有者も期日もない)。R5 も実行不能につき取り下げ。

否定 1 の確信度は 70% → **80%** へ引き上げ(criterion 1 が実データに支持体を持たないことが確定し、注入の便益側がほぼゼロと判明したため)。

## 3. D1〜D5 への立場

### D1:三つに分解すべきで、答えは三つとも異なる

- **(a) criterion 1(誤った一般則の明示): 本物の対立。反対する**(攻撃 1・2)。
- **(b) criterion 2(反復一貫性): 対立ではなく配置の問題。criterion 自体には賛成**。攻撃 7 の理由により集約側に置くほかない。
- **(c) 封印 vs 診断報告: 段階の違い。私が譲る**——「参考指標としても報告しない」を撤回。ファミリ内混同行列を診断として常時報告、ただし (i) KPI へ昇格させない、(ii) **必ずペアの ρ と識別可能率を併記**。

### D2:独立の対立ではない。D1 の含意である

手続き上の指摘: **a_r1.md に RL・報酬・部分報酬への言及は一箇所もない**(grep 済み)。「A は活用」を A の主張として攻撃しない。ファシリテータは帰属を確認されたい。分析上の立場: 下位区分を残すなら部分報酬は撤去すべきでない(曖昧領域 32.6% の恒常ゼロ報酬は RL で純ノイズ化)。識別可能粒度へ揃えるなら撤去が正しい。**私の立場は撤去。ただし D1 で A 案が採られるなら私の撤去主張は誤りになる。** 付言: 部分報酬はファミリ跨ぎ衝突③(91 件)には一切効かない。

### D3:私の r1 案(一律 sev2)は妥当でない。撤回する

ノイズ源は「深刻度が下位区分から導出されていること」。一律化はノイズと一緒に実在の勾配を捨てる。**修正案: 深刻度を原因由来から結果由来へ再定義**(最終解への影響・伝播・符号のみか——生成時に決定論的に計算可能で衝突経路に依存しない)。**裁定順序として、深刻度を RL 重みに使うか否かを先に確定させることを要求する。**

### D4:即時統合側へ立場を変更する。r1 の「G3+再生成後に再判定」を撤回する

理由 2 点(いずれも新しい再計算): **(i)** ρ=0.423 は共存領域外を分母に含めた希釈値。P(q·r<0)=0.5、q·r>0 側は検証器棄却で衝突構造ゼロ → 条件付き衝突率 362/(855×0.5) = **0.847** で自分の統合帯(≥0.7)に入る。**(ii)** 「識別可能」57.7% の大半は**偽の識別可能性**——q·r>0 側で識別できるのは競合オペレータが候補に入らないからで、「積が正なら概念」という生成器の適格性規則を覚えれば満点。実答案では転移率ゼロ。さらに G3 案への自己批判: 衝突候補を棄却して再生成すると、実答案で最も重要な領域(|q·r| を書く生徒)で**自信をもって間違えるモデル**を作る。r1 の復活条件 (i) は誤りだった。

**代替案(A 案とも私の r1 とも異なる): クラス単位の統合ではなく、領域単位のラベル粒度。** 各サンプルの GT を「その観測から識別可能な最も細かいラベル」とし、**再生成・再訓練・GPU 不要の再ラベルのみ**で実現。評価は識別可能粒度との完全一致とし、**識別不能な標本に下位区分を出したら過剰主張として不正解**。KPI は「区別できないときに区別できないと言えるか」を測り、教育的に正しい振る舞い(判らないのに『概念的誤解です』と言わない)と一致する。上限 1.0 になるので閾値 0.95 以上とし、**下位区分ラベルの被覆率を必ず併記**(併記しないと全ファミリ回答の退化解が満点)。

**事前登録した反証:** ρ_積(q·r<0 条件付き)の予測は **0.80〜0.90**。0.6 を下回れば D4 転向は誤りで r1 の G3 帯へ戻す。

### D5:条件付き採用(制約は今採用、新出力は範囲外として先送り)

**採用:「`errors[].type` には要素局所かつ観測から識別可能なラベルしか載せない」という制約。** **先送り: 概念診断の集約出力の設計**(中学生書き手という未決の外部依存に全面依存)。**ただし設計書に必ず記録: 下位区分の復活は技術条件ではなく未決の外部調達(中学生書き手)に律速される。**

## 4. ファシリテータへの検算要求

1. ③の再カウント(site==s2 条件付き)/ 2. ρ_積の q·r<0 条件付き再計算(予測 0.80〜0.90)/ 3. llama.cpp 側の統合後マクロ

## 5. 真の合意点

1. (c) 現行維持の否定。2. 必須 KPI を第1階層(統合 7 クラス)へ。3. 語りの注入((b) 単独)は合成ショートカット。4. 反復一貫性が概念診断の唯一の観測可能な根拠(相違は配置のみ)。5. 両ランタイムで必須値(平均で帳尻を合わせない)。6. n=3 のクラスで設計判断しない。7. 旧・新 evaluator の回帰テスト(同一 200 件の score/bbox 完全一致)。8. operator_id と深刻度は provenance として保持し VLM の種別 GT にしない。9. `reason` が verbalizer に渡らないというコード経路の認定。

---
# ラウンド 2.5: ファシリテータ検証済み事実・追補(V6〜V9、両パネリストに同一提供)

## V6. 条件付き ρ_積(B の R2 検算要求・事前登録予測の判定)

- **ρ_積 | q·r<0 = 362/430 = 0.842**。B の事前登録予測帯 0.80〜0.90 に**的中**(理論値 0.847)。
- q·r>0 側で mv=|q·r| だった件数 = **0**(検証器が gold 一致案を棄却する構造も B の主張どおり)。
- P(q·r<0) 実測 = 430/855 = 0.503。

## V7. 衝突候補③の再カウント(V1 の但し書きの訂正)

- op-arith-slip は正負の数ドメインに 987 件、**うち site=s2 が 504 件**。gen_core.py:237 の s2/s3 抽選は原文確認済み。**V1 の但し書き「変異サイトは通常別ステップ」はファシリテータの誤りであり撤回する。**
- 概念/符号・乗算側の mv=q·r±1,2 は 91/855 = 10.6%。この 91 件は arith-slip@s2(504 件)と同一サイト・同一形式で衝突し得る(**ファミリ間**曖昧プール、全誤りサンプルの ~1.3%)。

## V8. llama.cpp 側の統合後マクロ(B の検算要求 3)

nosys 公式 n=200 の行別ログを同一統合写像で再集計:

- **llama.cpp 統合後 7 クラスマクロ = 0.852**(内訳: 移項 0.952、符号・乗算 0.864、演算順序 1.000、**符号落とし 0.333**、加減 1.000、加減乗除 0.875、除算 0.938)
- HF 統合後 0.970 との差は大きい。主因は配備経路(量子化 mmproj/前処理)で劣化する符号系の微細視覚(符号落とし 0.333)であり、タキソノミ問題ではない。
- **含意: 「両ランタイムで必須値」の原則の下では、閾値 0.95 は現状不成立**(HF 0.970 ✓ / llama.cpp 0.852 ✗)。A の攻撃 3「0.95 は単一評価ログへの後付け」に実証が付いた。

## V9. 手続き上の訂正・確認

1. **D2 の帰属はファシリテータの誤り。** a_r1.md に RL・報酬・部分報酬への言及は存在しない(grep 確認)。r2_header の「A は活用」は brief_A の設計書引用をファシリテータが A の立場として誤帰属したもの。A の R2 における D2 論述は事後採用として扱う(A はラウンド 3 で自らの立場として維持するか撤回するかを表明されたい)。
2. **collection_plan.md の B 引用は逐語一致を確認**(13 行目の成人除外・50 行目の中学生調達未決・書き手 ID スキーマの既存)。
3. **B の上限再計算はファシリテータが独立に再導出して一致を確認**: 正解率最適 (4.833+1.044+0.957)/9 = **0.759**(実測 0.760)、マクロ最適 0.821。ただし注記: この「上限」は非衝突クラスとファミリ到達度に現行モデルの実測値を代入した条件付き上限であり、純粋な情報理論的上限ではない(実測との 3 桁一致は「下位区分の割当が純粋なタイブレークである」ことの強い整合性証拠として扱うのが正確)。
